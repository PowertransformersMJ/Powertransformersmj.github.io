# 🩺 05 — ESTADO GLOBAL (Heartbeat · Snapshot de salud del sistema)

> **Nodo neuronal: signos vitales.** Se **AUTO-CARGA** (junto a `CLAUDE.md` +
> `10-CORTO-PLAZO`). Responde *"¿en qué estado está el sistema AHORA, antes de
> tocar nada?"*. Lo lee el **Reflejo de Auto-auditoría (`CLAUDE.md §G.4`)** al arrancar.
>
> **Mantenimiento (Reflejo de Frescura §G.4)**: actualizar al cambiar versión,
> branch, build o al detectar/resolver un riesgo. **Tope ~25 líneas (§G.5)** — es un
> tablero, no una bitácora.

| Señal | Valor |
|---|---|
| **Build** | 🟢 `npm test` (lint + `node --test tests/*`) **1160/1160 verde** (2026-06-09, +multi-año TENDENCIA año a año: 1 gráfica por SUB-PRUEBA (par de devanados, no fusiona escalas), TODOS los años por defecto, orden cronológico + etiquetas uniformes + **desambiguación estrella/delta** + **"Resultados del informe" = UN informe a la vez con SELECTOR** (no apila; tablas por selección); validado con informes REALES 450108. **SFRA visible** (chip propio "· SFRA estrella" por tipo de ensayo; coeficientes de correlación por banda DL/T911 — las trazas dB-Hz del PDF son solo imagen, no se inventan; §28.10). **Scorecard añade SFRA + DFR** (criterio normativo; §28.11). **Tan δ de devanados CONDENSADO** en panel único filtrable (`tand-panel.js`: tendencia año tras año en BARRAS + filtros año/grupo/tensión/devanado + "Todos los años"; excluido del genérico; ADR-029). Vista "Por devanado" con **leyenda LIMPIA por informe + criterio normativo VISIBLE** (`CRIT` IEEE 62/C57.152, veredicto por barra y en caption; ADR-031). Bloque **"Análisis conforme a norma"**: sellos/emblemas ESTILIZADOS propios (NETA + IEEE, no logotipos oficiales) + `analizarTand` (veredicto multi-norma + peor medición + tendencia; ADR-032). **Overlay genérico "Demás pruebas" RETIRADO por completo** vía predicado `excluidaDelOverlay` (familias excitacion/relacion/resistencia/aislamiento/bushing/collar + regla `^otros:` para SFRA): la sección ya no se renderiza; **panel tan δ = única vista multi-año**. Datos y motor INTACTOS (scorecard evalúa todo); reversible (ADR-033/034). **Sección "Resultados del informe" CONSERVADA**; del detalle por informe solo se filtra el **bloque tan δ** (ya vive en su panel condensado) — bujes/excitación/relación/resistencia/aislamiento + selector intactos (ADR-036 corrige el sobre-retiro de ADR-035; L-51). Vista **Tip-up** (ΔFP=FP@alta−FP@baja por sección: ionización/PD vs humedad/tierra-núcleo; `svgTipUp`) + **caveat 20 °C** (informes sin T → se declara, no se inventa) — auditoría 🔵 con skill factor-potencia-aislamiento; + **localización del defecto por modo** (`localizacionDe`/`causaProbableDe`; gap #2 ADR-039) + **pendiente predictiva por sección/baseline-proxy** (#5/#6 ADR-040; #3 capacitancia descartada por artefacto). Auditoría FP CERRADA. **Modal de colisión por fecha abre AMBOS PDFs** (guardado + nuevo) para comparar antes de decidir (ADR-030). **Panel de CORRIENTE DE EXCITACIÓN propio** (`excitacion-panel.js`, espejo del tan δ; ADR-041): discriminación por NIVEL DE TENSIÓN (`nivelDe`: AT 66/110·MT 34.5·BT 13.8; "10 kV"=tensión de ensayo) + 5 vistas (Δ por TAP / patrón 2+1 / tendencia / por nivel / **tabla mA·W·ambos**) + **W de pérdidas** (`perdidasDe`) + criterio COMPARATIVO conforme a norma (NETA 2+1 / IEEE 62; sin umbral % duro). Patrón = FORMA (externas simétricas + central distinta); dirección HLH/LHL informativa — la central B es la MENOR en estrella Y delta por GEOMETRÍA del núcleo (L-53). 19 tests. **Vista "Resumen (todo)"** (valores mA + desviación + tablas, por defecto) + **gating de tablas** (solo al acotar a 1 año o 1 nivel → no satura) + fix `reset` (Sets en sitio, L-54) + **separación POR NIVEL DE TENSIÓN** (cada nivel: su gráfica de valores + su desviación + su criterio; ADR-042 §42.8). **1179/1179.** |
| **Versión** | `v2.4.1` + cerebro v1.0.0. Pruebas Eléctricas con IA: tablero **IA-primaria**, **veredicto 100% MULTI-NORMA** (valor vs cada norma + consolidado + recomendación, NUNCA texto IA). **Arco ADR-003→014 EN PRODUCCIÓN**: extracción IA + canal `extra`, multi-norma + diagnóstico, **bujes canónico**, **identidad/placa por informe** (trafo móvil doble config → aislamiento por clase del propio ensayo), tablas SIN col. "Evaluación/OK", **Tendencia alto nivel** (diagnóstico unidad + timeline + narrativa IA), biblioteca-hub, upsert por fecha, reproceso server-side + backfill instantáneo. Detalle → `99 §9..§14`. Secret `LLM_API_KEY` ok. |
| **Cache / SW** | n/a — `sw.js` es kill-switch (PWA offline desactivada a propósito). |
| **Branch activa** | `DESARROLLO-/-PROYECTO-MJ` (dev) → merge a `main` (producción). Verificado 2026-06-08. |
| **Producción** | `main` → GitHub Pages (`pages.yml`). Arco **ADR-029→040** (panel tan δ + auditoría FP) **EN PRODUCCIÓN** (PR #172, mergeado 2026-06-09). **Arco ADR-041** (panel corriente de excitación): el **panel** (`3ce25ae`) ya está **EN PRODUCCIÓN** (PR #173, mergeado 2026-06-10). El **CABLEADO al shell** (`7bb8b38`, `pruebas-electricas-shell.js`) ya está en `main`. **ADR-042** (vista "Resumen (todo)" + gating de tablas + fix `reset`) está **COMMITEADO en `DESARROLLO`, FALTA PUSH+PR a `main`** (lo dispara el director). Falta validar en la APP (tras Auth). Backend sin cambios. `git fetch` antes de afirmar estado (refs locales STALE). |
| **Deploys backend** | (1) `extraerPruebasElectricasIA` **DESPLEGADA** (southamerica-east1; **solo-CARGA** desde ADR-020 — el modo-reproceso se retiró). Robustez de transporte que SE CONSERVA (la carga de informes densos la usa): reintento con presupuesto (`functions/reintentos.mjs`, `intentoMaxMs`), timeout interno abortable (`conTimeoutAbortable`), **dispatcher de `undici@^6` SIN bodyTimeout** (fix "terminated", ADR-018), `timeoutSeconds:1500`/2GiB, cliente 1500s, `effort:high` (máxima calidad). Detalle ADR-015→020/L-44..L-48. (2) `narrativaTendenciaIA` **DESPLEGADA** (F3, sin PDF, default sonnet, 120s/512MiB). Secret `LLM_API_KEY` ok. Firestore `experimentalAutoDetectLongPolling` (L-38). **Flujo (ADR-005)**: Claude commitea+deploya; el **director pushea**. |

## ⚠️ Flags de riesgo activos
- **Flujo git (ADR-005)**: Claude commitea+deploya, el director pushea/mergea (runtime 403, L-01); nunca force-push a `main`. **Tablero + Tendencia F1-F3 consolidados** (ADR-009/010); **veredicto MULTI-NORMA** (ADR-011/012, L-36/L-37): cada prueba se evalúa contra CADA norma → panel por bloque con veredicto por norma + **consolidado conservador** + divergencias (caso testigo: 5 GΩ@110kV pasa piso NETA 5 GΩ / falla por clase 30 GΩ). Motor único `pruebas_electricas_multinorma.js` alimenta scorecard/KPI/matriz/timeline/badges + **capa de diagnóstico `pruebas_electricas_recomendaciones.js`** (sugerencia accionable por bloque cuando el veredicto no es aprobado limpio, conforme a las skills 04-diagnostico). El veredicto sale del VALOR vs norma, nunca del texto IA. **Identidad/placa por informe (ADR-014)**: aislamiento por la clase del PROPIO ensayo (trafo móvil 63.5/110 kV). **Sin riesgos abiertos.** ⚠️ Umbrales por clase MO.00418 + PI/DAR + banda C1 bujes siguen `verificar` (TODO-08). **"Reprocesar" RETIRADO** (ADR-020: costo > valor; re-extraer = re-subir el PDF). **Previsualización al colisionar por fecha** (ADR-021). Pendientes: TODO-01/02/08.

## 🧩 Sub-sistemas (resumen)
- Frontend estático (HTML/CSS/JS vanilla) ✅ · Firebase (Auth + Firestore + Storage) ✅ · Cloud Functions desplegables (F32) ✅ · Vercel `/api` ✅ · PWA/SW ⛔ desactivada.
