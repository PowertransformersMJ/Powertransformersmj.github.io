# Dependencias
node_modules/
.pnpm-store/
.npm/

# Logs
*.log
npm-debug.log*

# Entornos
.env
.env.local
.env.*.local

# Editores
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code (worktrees, sesiones, memoria local — nunca al repo)
# EXCEPCIÓN (decisión F3a 2026-07-18): settings.json SÍ se commitea — son los
# hooks del cerebro (brain-check --boot + session-handoff) y deben viajar con el repo.
.claude/*
!.claude/settings.json

# Artefactos de depuración / QA (PDFs reales de cliente, capturas) — NUNCA al repo público
Debug/

# Build / caches
.cache/
.parcel-cache/
.vercel/
dist/
build/

# Cloud Functions: copia AUTO-GENERADA del dominio (functions/prepare-deploy.mjs
# la regenera desde assets/js/domain/ en cada `firebase deploy --only functions`
# vía el hook predeploy). NUNCA se edita a mano ni se commitea (ADR-052 Ola 5,
# G119): commiteada solo duplicaba el dominio y ya había divergido stale.
functions/domain/

# Secretos / claves
*.key
*.pem
serviceAccount*.json
service-account*.json
*-service-account*.json
*.serviceaccount.json
gcp-*.json
firebase-adminsdk*.json

# Firebase (locales)
.firebase/
firebase-debug.log*
firestore-debug.log*
storage-debug.log*
ui-debug.log*
*-debug.log
.runtimeconfig.json

# Informes de cliente para consulta local (NO publicar en GitHub Pages — confidencial AFINIA)
# Carpetas de referencia por serial (450108, etc.). Quedan en disco para que Claude
# las consulte, pero NO se commitean (el user-page es público).
450108/

# 🧠 Cerebro — sidecars efímeros escritos por los hooks de sesión (nunca al repo)
docs/.handoff-auto.md
docs/.estado-auto.md
docs/.boot-marker
.claude/settings.local.json.BAK-*
# ⚠️ scripts/.kernel-version.json SÍ se commitea: es el sello de integridad del kernel (gate #0).

# ⚠️ CONFIDENCIAL / COPYRIGHT — NUNCA al repo público (ADR-052 Ola 0)
# El repo es user-page público (publica todo el root). Estas clases de archivo
# son datos reales de AFINIA o material de fabricante con copyright: van a
# Storage privado o a la bóveda local, NUNCA al repo.
# Manuales OEM / normas / material de estudio (raíz):
/OLTC*.pdf
/Rele*.pdf
/Valvula*.pdf
/bal_*.pdf
/5785291*.pdf
/D1816*.pdf
# Dashboards standalone con datos operativos reales (raíz):
/Seguimiento Limites SCADA.html
/SAIDI_SAIFI*.html
/Tablero Dinamico de Pruebas Electricas.html
/Cargabilidad Transformadores*.html
# Hojas de negocio con datos reales (raíz):
/Salud de Activos*.xlsx
/Gestion_Suministros_Transformadores*.xlsm
